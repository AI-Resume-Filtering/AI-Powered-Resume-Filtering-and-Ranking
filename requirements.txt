# AI-Powered Resume Filtering System - Requirements

# ============================================
# Resume Parser Module
# ============================================
pdfplumber>=0.9.0          # PDF parsing and text extraction

# ============================================
# NLP Engine Module
# ============================================
tqdm>=4.65.0               # Progress bar for batch processing

# ============================================
# Optional - For Cloud/Database Integration
# ============================================
# boto3>=1.26.0            # AWS S3 (uncomment if using cloud storage)
# pymongo>=4.3.0           # MongoDB (uncomment if using MongoDB)
# psycopg2-binary>=2.9.0   # PostgreSQL (uncomment if using PostgreSQL)
# mysql-connector-python>=8.0.0  # MySQL (uncomment if using MySQL)

# ============================================
# Optional - For REST API
# ============================================
# fastapi>=0.95.0          # REST API framework (uncomment if building API)
# uvicorn>=0.21.0          # ASGI server (uncomment if building API)
# python-multipart>=0.0.6  # File upload support (uncomment if building API)

# ============================================
# Python Version
# ============================================
# Requires: Python 3.8+

# ============================================
# Installation
# ============================================
# Basic (required):
#   pip install -r requirements.txt
#
# With API support:
#   pip install -r requirements.txt fastapi uvicorn python-multipart
#
# With AWS support:
#   pip install -r requirements.txt boto3
#
# With database support:
#   pip install -r requirements.txt pymongo  # or psycopg2-binary or mysql-connector-python